# Memory - 2026-02-13

## Pokemon Game - BUGS FIXED ✅

### Fixes Applied & Merged to Main

**1. AUTO_CUTSCENE_MOVE disabled (line 587)**
```javascript
// BEFORE: const AUTO_CUTSCENE_MOVE = qs('autocutmove', '1') === '1';
// AFTER:  const AUTO_CUTSCENE_MOVE = qs('autocutmove', '0') === '1';
```
- Fixes: Character walking automatically without player input

**2. Rotation bug fixed (line 2696)**
```javascript
// BEFORE: GS.dir = dir; was set BEFORE canMove() check
// AFTER:  GS.dir = dir; moved INSIDE if (canMove()) block
```
- Fixes: Character spinning continuously when blocked

**3. Missing tiles added to collision (lines 672-686)**
```javascript
const T = {
    // ... existing tiles ...
    ROOF_RED: 19, ROOF_BLUE: 20, ROOF_LAB: 21,
    WALL: 22, WINDOW: 23,
    GRASS_ALT: 24, PATH_ALT: 25, DOOR: 26
};

const SOLID = [
    T.WATER, T.TREE, T.ROOF_RED, T.ROOF_BLUE, T.ROOF_LAB,
    T.HOUSE_RED, T.HOUSE_BLUE, T.LAB, T.FENCE, T.NPC,
    T.WALL_IN, T.TABLE, T.SHELF, T.WALL, T.WINDOW
];
```
- Fixes: Walking through roofs, walls, windows

### Commits
- `1a993be` - Fix AUTO_CUTSCENE_MOVE & rotation
- `3086532` - Fix collision detection
- Merged to `main` via fast-forward

### Validation
- ci-check.sh: ✅ 21 maps, 55 beats, all preflight OK
- Server: Python HTTP on ports 8000/8001

### User Note
- User said: "stop calling Codex, do it yourself"
- Fixed by reading code directly, not external tools
- User needs hard refresh (Ctrl+Shift+R) to see fixes

---

## OpenClaw Update
- Updated from **2026.2.9** → **2026.2.12** (via `gateway update.run`)

## Codex Bridge
- Socket: `/tmp/codex-bridge.sock`
- Logs: `/home/ubuntu/.codex/log/codex-tui.log`
